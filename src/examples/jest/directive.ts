import { ClassName } from "./${file.name}";

describe("${className}", () => {
  it("shoud ...", () => {
    const directive = new ClassName();
    expect(directive).toBeTruthy();
  });

  it.todo("shoud ...");
});
